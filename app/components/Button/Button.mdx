import { Meta, Story, Canvas } from '@storybook/addon-docs'
import Button from './Button'
import * as ButtonStories from './Button.stories'

// Define o metadata do componente e associa a história
<Meta title="Button" of={ButtonStories} />

# Button

Este é o componente `Button`, que pode ser usado para criar botões reutilizáveis em sua aplicação.

## Exemplo de uso

Aqui está um exemplo de como utilizar o componente `Button` com suas props padrão.

<Canvas>
  <Story name="Primary">
    <ButtonStories.Primary />
    <ButtonStories.Secundary />
  </Story>
</Canvas>

## Props

O componente `Button` aceita as seguintes props:

- `children` (ReactNode): O conteúdo do botão, normalmente texto.
- Outras props que podem ser passadas diretamente para o botão.
